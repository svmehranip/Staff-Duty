const API_URL="https://script.google.com/macros/s/AKfycbxoN5GYrT2wtHgx2oDhvKhzTDBslMktbL5NHOLlHmuBmCRoxcU3wskVNkLn94PXZJOL/exec";
let state={user:null,staff:[],leaves:[]};
const app=document.getElementById("app"),nav=document.getElementById("nav"),toast=document.getElementById("toast");
const today=()=>{let d=new Date();return d.getFullYear()+"-"+String(d.getMonth()+1).padStart(2,"0")+"-"+String(d.getDate()).padStart(2,"0")};
function normDate(v){if(v==null)return"";let s=String(v).trim(),m=s.match(/^(\d{4})[-\/](\d{1,2})[-\/](\d{1,2})/);if(m)return m[1]+"-"+String(m[2]).padStart(2,"0")+"-"+String(m[3]).padStart(2,"0");m=s.match(/^(\d{1,2})[-\/](\d{1,2})[-\/](\d{4})/);return m?m[3]+"-"+String(m[2]).padStart(2,"0")+"-"+String(m[1]).padStart(2,"0"):s}
function fmt(v){let s=normDate(v),p=s.split("-");return p.length===3?p[2]+" "+["Jan","Feb","Mar","Apr","May","Jun","Jul","Aug","Sep","Oct","Nov","Dec"][+p[1]-1]+" "+p[0]:"Invalid date"}
function msg(t,ok=true){toast.innerHTML=`<div class="toastmsg" style="${ok?"":"background:#b42318"}">${ok?"✓":"!"} ${t}</div>`;setTimeout(()=>toast.innerHTML="",2400)}
document.addEventListener("pointerdown",e=>{let b=e.target.closest("button");if(b){b.classList.add("tap");setTimeout(()=>b.classList.remove("tap"),150)}});
async function api(action,data={}){if(API_URL.startsWith("PASTE_"))throw Error("Set API_URL in app.js first");let r=await fetch(API_URL,{method:"POST",body:JSON.stringify({action,...data})});return r.json()}
async function sync(){try{let[s,l]=await Promise.all([api("staff"),api("leaves")]);state.staff=s.staff||[];state.leaves=l.leaves||[];localStorage.setItem("sd6",JSON.stringify(state))}catch(e){}}
function isOwner(){return state.user&&String(state.user.role).toLowerCase().includes("owner")}
function login(){nav.innerHTML="";app.innerHTML=`<div class="login"><div class="loginbox"><div class="logo">StaffDuty</div><p class="muted">Leave & duty management</p><label>Username<input id="un"></label><label>PIN<input id="pin" type="password"></label><button class="primary full" onclick="doLogin()">Login</button></div></div>`}
async function doLogin(){try{let r=await api("login",{username:un.value,pin:pin.value});if(!r.ok)return msg(r.error,false);state.user=r.user;sessionStorage.setItem("sdUser",JSON.stringify(r.user));await sync();go("home")}catch(e){msg(e.message,false)}}
function shell(page,title,body){app.innerHTML=`<div class="wrap"><header class="top"><div class="brand">StaffDuty</div><div class="topuser">${state.user.name} · ${state.user.role}</div></header><main class="content"><div class="title">${title}</div>${body}</main></div>`;nav.innerHTML=[["home","🏠","Home"],["calendar","📅","Calendar"],["apply","＋","Leave"],["approvals","✓","Approval"],["more","☰","More"]].map(x=>`<button class="${page===x[0]?"navOn":""}" onclick="go('${x[0]}')">${x[1]}<small>${x[2]}</small></button>`).join("")}
function leaveCard(x,remove=false){return `<div class="card"><div class="row"><b class="name">${x.Staff||"-"}</b><b class="date">${fmt(x.LeaveDate)}</b></div><p class="muted">Duty by <b>${x.DutyBy||"-"}</b><br>Reason: ${x.Reason||"-"}</p><span class="badge ${String(x.Status||"").toLowerCase()}">${x.Status||""}</span>${remove?`<div style="margin-top:10px"><button class="danger full" onclick="removeLeave('${String(x.LeaveID)}')">Remove leave</button></div>`:""}</div>`}
function home(){shell("home","Dashboard",`<div class="hero"><div><div class="muted">Absent today</div><b>${state.leaves.filter(x=>normDate(x.LeaveDate)===today()&&x.Status==="Approved").length}</b></div><div class="heroIcon">👥</div></div><div class="switch"><button id="ba" class="on" onclick="showList('a')">Absent Today</button><button id="bu" onclick="showList('u')">Upcoming Leave</button></div><div id="list"></div>`);showList("a")}
function showList(k){ba.classList.toggle("on",k==="a");bu.classList.toggle("on",k==="u");let x=k==="a"?state.leaves.filter(x=>normDate(x.LeaveDate)===today()&&x.Status==="Approved"):state.leaves.filter(x=>normDate(x.LeaveDate)>=today()&&x.Status!=="Rejected").sort((a,b)=>normDate(a.LeaveDate).localeCompare(normDate(b.LeaveDate)));list.innerHTML=x.length?x.map(x=>leaveCard(x,k==="u"&&isOwner())).join(""):'<div class="empty">No leave records.</div>'}
function apply(){let me=state.user.name,o=state.staff.filter(x=>x.Name!==me);shell("apply","Report Leave",`<div class="card"><label>Staff<select id="st"><option value="${me}">${me}</option></select></label><label>Leave date<input id="dt" type="date" min="${today()}"></label><label>Duty will be done by<select id="cv"><option value="">Select replacement</option>${o.map(x=>`<option value="${x.Name}">${x.Name}</option>`).join("")}</select></label><label>Reason<textarea id="rs" rows="3"></textarea></label><button class="primary full" onclick="submitLeave()">Submit for approval</button></div>`)}
async function submitLeave(){let date=String(dt.value||"").trim();if(!/^\d{4}-\d{2}-\d{2}$/.test(date))return msg("Please select a valid date",false);if(date<today())return msg("Please select a future date",false);if(!cv.value)return msg("Select replacement staff",false);try{let r=await api("createLeave",{staff:st.value,date:date,cover:cv.value,reason:rs.value});if(!r.ok)return msg(r.error||"Unable to submit leave",false);await sync();msg("Leave submitted ✓");go("home")}catch(e){msg(e.message,false)}}
function approvals(){if(!isOwner())return shell("approvals","Owner Approval",'<div class="empty">Owner access required.</div>');let p=state.leaves.filter(x=>x.Status==="Pending");shell("approvals","Owner Approval",`${p.length?'<h3>Pending</h3>'+p.map(x=>`<div class="card"><div class="row"><b>${x.Staff}</b><b class="date">${fmt(x.LeaveDate)}</b></div><p class="muted">Duty by ${x.DutyBy}<br>${x.Reason||"-"}</p><div class="actions"><button class="primary" onclick="approve('${x.LeaveID}')">✓ Approve</button><button class="danger" onclick="reject('${x.LeaveID}')">Reject</button></div><button class="danger full" style="margin-top:8px" onclick="removeLeave('${x.LeaveID}')">Remove leave</button></div>`).join(""):'<div class="empty">No pending approvals.</div>'}<h3>Approved leaves</h3>${state.leaves.filter(x=>x.Status==="Approved").map(x=>leaveCard(x,true)).join("")||'<div class="empty">No approved leaves.</div>'}`)}
async function approve(id){let r=await api("approve",{id,username:state.user.username});if(!r.ok)return msg(r.error,false);await sync();msg("Approval recorded ✓");go("approvals")}
async function reject(id){let r=await api("reject",{id,username:state.user.username});if(!r.ok)return msg(r.error,false);await sync();msg("Leave rejected");go("approvals")}
async function removeLeave(id){if(!isOwner())return msg("Owner access required",false);if(!id)return msg("Leave ID missing",false);if(!confirm("Remove this leave?"))return;try{let r=await api("removeLeave",{id:String(id),username:String(state.user.username)});if(!r.ok)return msg(r.error||"Unable to remove leave",false);await sync();msg("Leave removed ✓");go("home")}catch(e){msg(e.message,false)}}
function calendar(){shell("calendar","Leave Calendar",'<div class="empty">Use Home → Upcoming Leave to view and manage future leave.</div>')}
function more(){shell("more","More",`<div class="card"><b>${state.user.name}</b><p class="muted">${state.user.role}</p></div><div class="card"><b>Change password</b><label>Current PIN<input id="oldpin" type="password"></label><label>New PIN<input id="newpin" type="password"></label><label>Confirm<input id="confpin" type="password"></label><button class="primary full" onclick="changePassword()">Change password</button></div><div class="card"><button class="secondary full" onclick="logout()">Log out</button></div>`)}
async function changePassword(){if(!oldpin.value||!newpin.value||newpin.value!==confpin.value)return msg("Check PIN fields",false);let r=await api("changePassword",{username:state.user.username,oldPin:oldpin.value,newPin:newpin.value});if(!r.ok)return msg(r.error,false);msg("Password changed ✓")}
function logout(){state.user=null;sessionStorage.removeItem("sdUser");login()}
async function go(p){if(!state.user)return login();({home,calendar,apply,approvals,more}[p])()}
try{let x=JSON.parse(localStorage.getItem("sd6")||"{}");state.staff=x.staff||[];state.leaves=x.leaves||[]}catch(e){}
let su=sessionStorage.getItem("sdUser");if(su){try{state.user=JSON.parse(su);go("home");sync()}catch(e){login()}}else login();if("serviceWorker"in navigator)navigator.serviceWorker.register("sw.js");